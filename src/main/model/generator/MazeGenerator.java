package model.generator;

import model.maze.ImmutableMaze;

// Generates randomized Mazes of arbitrary size
public class MazeGenerator {

    // REQUIRES: size must be in [5, 10]
    // EFFECTS: produce a square maze with specified side-length
    public static ImmutableMaze generateMaze(int size) {
        // TODO: add link a to the wikipedia page for the algorithm we are implementing
        return null; // stub
    }
    
}
